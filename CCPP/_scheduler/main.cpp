#include <iostream>
#include <queue>
#include <string>

using namespace std;

struct job {
  // 任务名称
  string name;
  // 优先级
  int priority;
  // 执行时间
  int execution_time;

  job(string n, int p, int et) : name(n), priority(p), execution_time(et) {}

  
};

int main() {


  return 0;
}
